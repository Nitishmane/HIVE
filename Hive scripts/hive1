use nitish;
create table weather(stationid STRING, maxtemp int,mintemp int,dewpoint int, precipitation int,latitude STRING, longitude STRING )
            row format delimited
            fields terminated by ',';
