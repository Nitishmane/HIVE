use nitish;
create table weather2(stationid STRING,year int, maxtemp int,mintemp int,dewpoint int, precipitation int,latitude string, longitude string)
            row format delimited
            fields terminated by ',';
