use nitish;
create table weather(stationid STRING,year int, maxtemp int,mintemp int,dewpoint int, precipitation int,latitude int, longitude int)
            row format delimited
            fields terminated by ',';
